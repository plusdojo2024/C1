/* レシピ用SQL */
SELECT RECIPES.ID, RECIPES.RIKISHI_ID, RECIPES.CATEGORY, RECIPES.RECIPE_PIC, RECIPES.RECIPI_NAME, RECIPES.RECIPI_LINK, RIKISHIES.RIKISHI_NAME, RIKISHIES.SUMO_STABLE_NAME

/* RECIPESテーブルとRIKISHIESテーブルを結合 */
FROM RECIPES
INNER JOIN RIKISHIES
ON RECIPES.RIKISHI_ID = RIKISHIES.id

/* 取得した検索データで検索 */
WHERE RIKISHIES.RIKISHI_NAME LIKE ? AND RIKISHIES.SUMO_STABLE_NAME LIKE ? AND RECIPES.CATEGORY LIKE ?;